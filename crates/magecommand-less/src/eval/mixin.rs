//! Mixin resolution (plan §2.5, §4.4): candidate lookup, arity + pattern
//! matching, guard eval under both `default()` polarities, param binding
//! (`@arguments`/variadic/named), `!important` propagation, recursion + depth
//! cap. Stub until Phase 2.
