//! `:extend` (plan §2.8, §4.5): finder → chaining fixpoint (cap 100) → replace;
//! `all` fragment match; `@media` scoping incl. `(reference)` media stacks. Runs
//! after eval + JoinSelector + MarkVisibleSelectors. Stub until Phase 4.
