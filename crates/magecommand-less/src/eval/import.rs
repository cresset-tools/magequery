//! Two-stage `@import` (plan §2.9, §4.5): pre-eval fetch/parse, in-eval
//! position-preserving splice; option handling (`once`/`reference`/`optional`/
//! `inline`/`layer`); `strictImports`; the `magento_mode` directive expansion
//! via `ImportResolver::magento_import` (plan §7.1). Stub until Phase 4.
