//! Number fns: `percentage, min, max, convert(v,unit), pi, mod, pow`.
//! `min`/`max` DUAL behavior — reduce compatible-unit args, else pass through as
//! literal CSS `min()`/`max()` (plan §2.7, §4.8). Stub until Phase 3.
