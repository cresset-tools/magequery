//! Unit-conversion tables (plan §1): `length {m,cm,mm,in,px,pt,pc}`,
//! `duration {s,ms}`, `angle {rad,deg,grad,turn}`. Generated-from-source in
//! Phase 3. Empty until then.

/// `(unit, factor-to-base)` within a unit family. Generated-from-source (plan §1).
pub static LENGTH: &[(&str, f64)] = &[];
/// Duration family conversion factors.
pub static DURATION: &[(&str, f64)] = &[];
/// Angle family conversion factors.
pub static ANGLE: &[(&str, f64)] = &[];
